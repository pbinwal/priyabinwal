# Enable GFM (GitHub Flavored Markdown) for kramdown to support strikethrough and other features
kramdown:
  input: GFM
# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Priya Binwal
email: your-email@example.com

# GitHub Pages settings
# Set your repository name as baseurl if hosted at https://username.github.io/repo-name/
baseurl: "/priyabinwal" # <-- use your repo name here
url: "https://pbinwal.github.io" # <-- your GitHub Pages domain

twitter_username: jekyllrb
github_username: pbinwal

# Build settings
theme: jekyll-theme-cayman
plugins:
  - jekyll-feed

# Default layouts for collection items
defaults:
  - scope:
      path: ""
      type: "prose"
    values:
      layout: "page"
  - scope:
      path: ""
      type: "science"
    values:
      layout: "page"
  - scope:
      path: ""
      type: "anubhuti"
    values:
      layout: "page"
  - scope:
      path: ""
      type: "IgnoRants"
    values:
      layout: "page"

# Collections
# Collections
collections:
  # anbhigya:
  #   output: true
  #   permalink: /anbhigya/:title/
  prose:
    output: true
    permalink: /prose/:title/
  science:
    output: true
    permalink: /science/:title/
  anubhuti:
    output: true
    permalink: /anubhuti/:title/
  IgnoRants:
    output: true
    permalink: /ignorants/:title/


# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
